services:
  metube:
    container_name: metube
    image: ghcr.io/alexta69/metube:latest
    ports:
      - 90:8081
    volumes:
      - ./metube/downloads:/downloads
      - ./metube/cookies:/cookies
    restart: unless-stopped
    
  watchtower:
    image: containrrr/watchtower
    container_name: watchtower
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock

  postgres:
    image: postgres:16-alpine
    container_name: postgres
    restart: always
    volumes:
      - ./pgdata:/var/lib/postgresql/data

  linkwarden:
    container_name: linkwarden
    restart: always
    image: ghcr.io/linkwarden/linkwarden:latest
    ports:
      - 100:3000
    volumes:
      - ./linkwarden/data:/data/data
    depends_on:
      - postgres
      
  stash:
    image: stashapp/stash:latest
    container_name: stash
    restart: unless-stopped
    ports:
      - 9999:9999
    logging:
      driver: "json-file"
      options:
        max-file: "10"
        max-size: "2m"
    volumes:
      - /etc/localtime:/etc/localtime:ro
      - ./stash/config:/root/.stash
      - ./stash/data:/data
      - ./stash/gallery:/gallery
      - ./stash/metadata:/metadata
      - ./stash/cache:/cache
      - ./stash/blobs:/blobs
      - ./stash/generated:/generated
      
  tinymediamanager:
    image: tinymediamanager/tinymediamanager:latest
    container_name: tinymediamanager
    volumes:
      - ./tinymediamanager/data:/data
      - ./tinymediamanager/media:/media
      - ./tinymediamanager/app/addons:/app/addons
    ports:
      - 5900:5900
      - 4000:4000
    restart: unless-stopped

  jdownloader:
    image: jlesage/jdownloader-2
    container_name: jdownloader
    restart: unless-stopped
    ports:
      - 5800:5800
    volumes:
      - ./jdownloader/config:/config:rw
      - ./jdownloader/downloads:/output:rw
